# Gate 1 — API Gateway

!!! note "Status: planned (Milestone M1)"
    Deploy a sample API and apply JWT/OAuth-style auth and rate limiting — all declarative config reconciled by ArgoCD. The GitOps story is the headline here.

## Coming here

- Deploy a sample API behind Traefik Hub.
- Apply authentication (JWT/OAuth-style) declaratively.
- Apply rate limiting declaratively.
- Show ArgoCD reconciling the config from Git.
- Demonstrate an allowed vs. rejected request.
