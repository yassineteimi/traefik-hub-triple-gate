# Gate 3 — MCP Gateway

!!! note "Status: planned (Milestone M3)"
    Deploy the MCP server (`mcp-ecommerce-agent`) behind the MCP Gateway and apply JWT auth + TBAC so only specific identities can call specific tools.

## Coming here

- Deploy the MCP server behind the MCP Gateway.
- Apply JWT authentication.
- Apply **TBAC** (tool-based access control).
- Demonstrate a **blocked vs. allowed** tool call by identity.
